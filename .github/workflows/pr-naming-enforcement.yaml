name: 'PR Title Checker'
on: 
  pull_request:
    types: [edited, opened, synchronize, reopened]

jobs:
  title-check:
    runs-on: ubuntu-latest
    steps:
      - name: Pull Request Title Check
        uses: naveenk1223/action-pr-title@master
        id: titlecheck
        with:
          regex: '.+' # Regex the title should match.
          allowed_prefixes: 'feature,fix,bugfix,patch,enhancement,hotfix' # title should start with the given prefix
          prefix_case_sensitive: false # title prefix are case insensitive
          min_length: 5 # Min length of the title
          max_length: 20 # Max length of the title

      # runs if previous jobs failed and test-app was not successful (failure/cancelled)
      - name: Close Pull Request and Comment
        if: failure() && steps.titlecheck.outcome != 'success'
        uses: peter-evans/close-pull@v2
        with:
          pull-request-number: ${{ github.event.number }}
          comment: Auto-closing pull request -- Reopen Pull Request with Correct Title
          delete-branch: false

  # close-pullrequest:
  #   if: job.title-check.steps.titlecheck.status == failure()
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: superbrothers/close-pull-request@v3
  #       with:
  #         # Optional. Post a issue comment just before closing a pull request.
  #         comment: "We do not accept PRs. If you have any questions, please feel free to contact us."